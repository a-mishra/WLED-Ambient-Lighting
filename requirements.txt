# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
PyYAML>=6.0
scipy>=1.11.0

# Raspberry Pi camera (install only on Pi)
# picamera2>=0.3.12

# Simulator display (optional — only needed for simulator.wled.display=window)
# pygame>=2.5.0

# Testing
pytest>=7.4.0
